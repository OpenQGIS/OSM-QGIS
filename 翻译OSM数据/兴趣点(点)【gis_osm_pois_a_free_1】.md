**gis_osm_pois_a_free_1**<br>
注：属性表表头为fclass

~~~
CASE 
    WHEN "fclass" = 'alpine_hut' THEN '阿尔卑斯小屋'
    WHEN "fclass" = 'apartment' THEN '公寓'
    WHEN "fclass" = 'artwork' THEN '艺术品'
    WHEN "fclass" = 'attraction' THEN '景点'
    WHEN "fclass" = 'bank' THEN '银行'
    WHEN "fclass" = 'bar' THEN '酒吧'
    WHEN "fclass" = 'bench' THEN '长凳'
    WHEN "fclass" = 'bicycle_rental' THEN '自行车出租'
    WHEN "fclass" = 'bicycle_parking' THEN '自行车停车场'
    WHEN "fclass" = 'biergarten' THEN '啤酒花园'
    WHEN "fclass" = 'bus_stop' THEN '公交车站'
    WHEN "fclass" = 'cafe' THEN '咖啡馆'
    WHEN "fclass" = 'casino' THEN '赌场'
    WHEN "fclass" = 'cinema' THEN '电影院'
    WHEN "fclass" = 'clinic' THEN '诊所'
    WHEN "fclass" = 'college' THEN '学院'
    WHEN "fclass" = 'community_centre' THEN '社区中心'
    WHEN "fclass" = 'courthouse' THEN '法院'
    WHEN "fclass" = 'dentist' THEN '牙医'
    WHEN "fclass" = 'doctor' THEN '医生'
    WHEN "fclass" = 'fast_food' THEN '快餐'
    WHEN "fclass" = 'ferry_terminal' THEN '渡轮码头'
    WHEN "fclass" = 'fuel' THEN '加油站'
    WHEN "fclass" = 'hospital' THEN '医院'
    WHEN "fclass" = 'hotel' THEN '酒店'
    WHEN "fclass" = 'library' THEN '图书馆'
    WHEN "fclass" = 'marketplace' THEN '市场'
    WHEN "fclass" = 'museum' THEN '博物馆'
    WHEN "fclass" = 'parking' THEN '停车场'
    WHEN "fclass" = 'pharmacy' THEN '药店'
    WHEN "fclass" = 'police' THEN '警察局'
    WHEN "fclass" = 'post_office' THEN '邮局'
    WHEN "fclass" = 'pub' THEN '酒馆'
    WHEN "fclass" = 'restaurant' THEN '餐厅'
    WHEN "fclass" = 'school' THEN '学校'
    WHEN "fclass" = 'taxi' THEN '出租车'
    WHEN "fclass" = 'theatre' THEN '剧院'
    WHEN "fclass" = 'toilets' THEN '厕所'
    WHEN "fclass" = 'university' THEN '大学'
    WHEN "fclass" = 'veterinary' THEN '兽医'
    WHEN "fclass" = 'zoo' THEN '动物园'
    ELSE '其他'
    /* Copyright by OpenQGIS */
END
~~~
